# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build first-monorepo`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t first-monorepo`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system first-monorepo && \
          adduser --system -G first-monorepo first-monorepo

COPY dist/apps/first-monorepo first-monorepo/
RUN chown -R first-monorepo:first-monorepo .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix first-monorepo --omit=dev -f install

CMD [ "node", "first-monorepo" ]
