# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build first-monorepo`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3001:3001 -t first-monorepo`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3001

WORKDIR /workspace

COPY ../../. ./

RUN  npm install

RUN npm install -g nodemon
RUN npm install -g typescript

WORKDIR /workspace/apps/first-monorepo

# CMD [ "node", "first-monorepo" ]
